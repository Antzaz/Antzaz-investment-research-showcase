name: Pre-release Pages deployment disabled

# Local-only staging branch. Intentionally never deploys to GitHub Pages.
on:
  push:
    branches: [__local_preview_only__]

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "pre-release-showcase is local preview only"
