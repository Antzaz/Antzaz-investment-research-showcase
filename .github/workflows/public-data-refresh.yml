name: Pre-release public-data refresh disabled

# Local-only staging branch. It must never refresh or push data into main.
on:
  push:
    branches: [__local_preview_only__]

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "pre-release-showcase never writes to main"
